CREATE TABLE [dbo].[PARTO]
(
	[PK_FK_NUMERO_TRAZABLE_VACA]	INT				NOT NULL,
	[FK_NUMERO_TRAZABLE_TORO]		INT				NULL,
	[PK_FECHA]						DATETIME		NOT NULL,
	[MUERTE_PREMATURA]				BIT				NOT NULL,
	[SEXO]							CHAR			NOT NULL,
	[CAUSA_ABORTO]					NVARCHAR(MAX)	NULL,

	CONSTRAINT CK_PK_PARTO PRIMARY KEY CLUSTERED (PK_FK_NUMERO_TRAZABLE_VACA, PK_FECHA),
	CONSTRAINT CK_PK_FK_NUMERO_TRAZABLE_VACA_PARTO FOREIGN KEY(PK_FK_NUMERO_TRAZABLE_VACA)
		REFERENCES dbo.VACA(PK_NUMERO_TRAZABLE) ON UPDATE CASCADE ON DELETE CASCADE,
	CONSTRAINT CK_PK_FK_NUMERO_TRAZABLE_TORO_PARTO FOREIGN KEY(FK_NUMERO_TRAZABLE_TORO)
		REFERENCES dbo.TORO(PK_NUMERO_TRAZABLE) ON UPDATE CASCADE ON DELETE SET NULL,
	CONSTRAINT CK_CHECK_SEXO CHECK (SEXO IN ('H', 'M'))
)
